rm -f .git/index.lock------sirve para borrar indexlock de ese directorio

git init----------para iniciar

git log-----------muestra quien esta lgeado

git status------------muestra las localizaciones con diferencias entre el workspace o commit head actual y el index

git diff--------------las diferencias modificaciones no añadidas

git commit -m "mensaje de modificacion"----------------añade la modificacion salvo los que no esten registrados

git add .----------------resgistra todos los archivos, sino remmplazar el punto con el directorio a añadir



