//definimos la funcion sumar

function sumar(a,b) {
    var resultado = a+b;  //almaceno el valor en resultado

    return resultado;    //retorna resultado que se almaceno en la variable suma
}

suma = sumar(3,4);       // variable suma
console.log(suma);


suma2 = sumar(5,6)
suma3 = sumar(7,6)



//otra manera 


function sumar(a,b) {
    return a+b;    
}