//es como for pero se usa cuando no se sabe la cantidad de iteraciones que se va a hacer durante el ciclo
//por ejemplo para una base de datos, no se sabe si hay uno o un millon.


var x = 10;

while (x > 0) {
    console.log("la variable vale"+x);
    x--;
}